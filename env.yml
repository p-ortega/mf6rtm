name: mf6rtm-dev
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pandas>=2.0
  - geopandas 
  - numpy < 2.0
  - matplotlib 
  - pip:
       - flopy
       - pyemu
       - -r requirements_dev.txt
